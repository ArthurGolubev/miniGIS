kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: minigis-pvc
  labels:
    app: minigis-app
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi